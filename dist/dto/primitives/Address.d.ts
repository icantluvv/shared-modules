export declare class Address {
    address: string;
    city: string;
    country: string;
    latitude: number;
    longitude: number;
    constructor(address: string, city: string, country: string, latitude: number, longitude: number);
}
